/**
 * Re-export useTheme hook from theme-context.ts
 * This is just a convenience export to follow the hooks pattern,
 * but all of the functionality is in the theme-context.ts file.
 */
export { useTheme } from '@/contexts/theme-context';
